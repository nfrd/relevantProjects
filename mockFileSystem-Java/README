The code that I contributed to the file system is in the files, mkdir.java, cp.java, pushd.java, and popd.java.
Those files contain code that execute the same way as the commands in Unix. 

Executing the JShell.java shows the capabilities of the filesystem.

